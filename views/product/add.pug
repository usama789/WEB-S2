extends ../layout

block content
    h1 Add New Product
    form(action="/product/add",method="post")
        .form-group
            label(for="code") Course Name
            input#code(type="text", class="form-control", name="cname")
        .form-group
            label(for="size") Course id
            input#size(type="text", class="form-control", name="cid")
        .form-group
            label(for="price") Course Duration
            input#price(type="text", class="form-control", name="cduration")
        .form-group
            label(for="stuff") Course Fee
            input#stuff(type="text", class="form-control", name="cfee")
        input(type="submit", class="btn btn-info")
    